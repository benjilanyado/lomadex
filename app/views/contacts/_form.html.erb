<%= simple_form_for @contact do |f| %>
	<%= f.association :list, 
		collection: current_user.lists.all %>
	<%= f.input :name %>
	<%= f.input :email %>
	<%= f.input :link %>
	<%= f.input :phone %>
	<%= f.button :submit %>
<% end %>